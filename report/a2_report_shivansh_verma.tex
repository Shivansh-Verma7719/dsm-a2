\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{geometry}
\geometry{
    top=1in,
    bottom=0.5in,
    left=0.5in,
    right=0.5in
}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{float}
\usepackage{setspace}

\title{\textbf{DSM A2 Part 1 Final Report: Yelp Open Dataset}}
\author{Shivansh Verma}
\date{\today}

\begin{document}
\maketitle

\tableofcontents
\newpage


\section{Data Acquisition and MongoDB Schema Design}

\subsection*{1. Core ETL Logic \& Data Loading}
The extraction, transformation, and loading (ETL) pipeline was developed in Python utilizing the \texttt{pymongo} library. Instead of a direct import mirroring the exact JSON constraints, we apply specific transformations defined in \texttt{etl/load\_yelp\_mongo.py}. The pipeline streams large \texttt{.json} files in batches, converts date strings into MongoDB-compatible Date values, restructures \texttt{checkin.date} into a \texttt{dates} array, normalizes user \texttt{friends} and \texttt{elite} into arrays, and maps Yelp IDs to MongoDB \texttt{\_id} where appropriate.

The script runs sequentially via:
\begin{verbatim}
python3 etl/load_yelp_mongo.py --data-dir ../data
\end{verbatim}

\subsection*{2. Collection Structure and Inter-relations}
The schema was modeled strictly based on the relational and analytical needs of the provided dataset parameters. Instead of mimicking the six source JSON files 1:1, the design separates bounded attributes (embedded) from unbounded elements (referenced) to prevent hitting MongoDB's 16MB document size limit as relationships scale.

Below is an overview of the five primary collections, their respective roles, document identifiers, and referential structures.

\subsubsection*{Businesses Collection}
\textbf{Purpose:} Acts as the central anchor for the database, housing core attributes, categorizations, and static metadata for various commercial locations.\\
\textbf{Identifier Field:} \texttt{\_id} (String, mapped directly from Yelp's native \texttt{business\_id}).\\
\textbf{Linking Relationships:} Is the parent entity. Other collections (like reviews, tips, and checkins) reference a business by its \texttt{\_id}.\\
\textbf{Key Fields:} \texttt{name}, \texttt{city}, \texttt{stars}, \texttt{review\_count}, \texttt{is\_open}. Attributes like \texttt{categories} (Array) and \texttt{hours} (Object) are strictly bound to the business and do not continuously grow, making them ideal to embed directly for localized queries.

\subsubsection*{Users Collection}
\textbf{Purpose:} Stores user metadata, engagement metrics, and social network ties.\\
\textbf{Identifier Field:} \texttt{\_id} (String, mapped directly from Yelp's native \texttt{user\_id}).\\
\textbf{Linking Relationships:} The \texttt{friends} field contains an array of strings referencing other \texttt{\_id} fields within the same collection (N:M self-referential). It is also globally referenced by the Reviews and Tips collections.\\
\textbf{Key Fields:} \texttt{name}, \texttt{yelping\_since} (Date), \texttt{elite} (Array), \texttt{average\_stars}, and various metrics. Due to the scale of social connectivity, \texttt{friends} arrays are populated strictly via references rather than embedded documents to avoid extreme growth amplification.

\subsubsection*{Reviews Collection}
\textbf{Purpose:} Logs independent evaluations made by users on specific businesses.\\
\textbf{Identifier Field:} \texttt{\_id} (String, mapped from \texttt{review\_id}).\\
\textbf{Linking Relationships:} Contains \texttt{user\_id} and \texttt{business\_id} fields, which inherently reference the Users and Businesses collections (N:1 mapping to both).\\
\textbf{Key Fields:} \texttt{stars}, \texttt{date} (Date object), \texttt{text}, \texttt{useful}. Referencing is strictly necessary here. If reviews were embedded within a user or business, heavily trafficked locations could easily crash string limitations.

\subsubsection*{Checkins Collection}
\textbf{Purpose:} Tracks longitudinal visitation trends mapped over time.\\
\textbf{Identifier Field:} \texttt{\_id} (String, mapped directly from \texttt{business\_id}), forming a rigid 1:1 schema extension with the Business entity.\\
\textbf{Linking Relationships:} Because the \texttt{\_id} maps to \texttt{business\_id}, it explicitly references the Businesses collection.\\
\textbf{Key Fields:} \texttt{dates} (Array of Date objects). Although checkins are technically bounded to a business, separating them into a new collection prevents the core business documents from massive index bloat generated by extremely large, actively expanding sub-arrays of timestamps.

\subsubsection*{Tips Collection}
\textbf{Purpose:} Captures smaller, qualitative offhand recommendations distinct from formal reviews.\\
\textbf{Identifier Field:} \texttt{\_id} (Auto-generated assigned by MongoDB).\\
\textbf{Linking Relationships:} Utilizes distinct \texttt{user\_id} and \texttt{business\_id} fields to bridge across the Users and Businesses collections.\\
\textbf{Key Fields:} \texttt{text}, \texttt{date} (Date object), \texttt{compliment\_count}. Like reviews, tips are unconstrained and inherently must maintain a normalized cross-reference.

\subsection*{3. Schema Design Justifications (Embedding vs. Referencing)}

\subsubsection*{(a) Relationship Design Choices}
\begin{itemize}
    \item \textbf{Business $\rightarrow$ Attributes, Categories, Hours (Embedded):} These entities inherently map 1:1 to a specific business. Their data volume is strictly bounded (e.g., there are only 7 days in a week for hours, and a finite set of attributes). Embedding these properties prevents expensive cross-collection joins just to retrieve basic business profile information.
    \item \textbf{User $\rightarrow$ Friends (Referenced):} The Yelp social graph is massive and highly interconnected (N:M). Embedding a user's friends list as full documents would exponentially duplicate data. Even embedding an array of \texttt{user\_id} strings risks exceeding MongoDB's 16MB document size limit for highly active "elite" users. Thus, referencing is strictly required.
    \item \textbf{Business $\rightarrow$ Reviews \& Checkins (Referenced):} Yelp reviews and checkins grow unboundedly over time (1:N mapping). Embedding thousands of reviews directly inside a single \texttt{business} document would inevitably breach the 16MB limit, severely degrading read/write performance long before failing entirely. Keeping these in standalone collections referencing \texttt{business\_id} is the only scalable approach.
    \item \textbf{User $\rightarrow$ Reviews \& Tips (Referenced):} Similar to businesses, a highly active user can generate an unbounded number of reviews and tips over their tenure. These must uniquely exist in their own collections, maintaining references back to the contributing \texttt{user\_id}.
\end{itemize}

\subsubsection*{(b) Read and Write Trade-offs}
\textbf{Embedding Trade-offs (Business Attributes/Categories/Hours):}
\begin{itemize}
    \item \textbf{Read Advantage:} Yields unmatched read performance. A single query to the Businesses collection retrieves the entire business profile, navigating latency from \texttt{\$lookup} joins.
    \item \textbf{Write Disadvantage:} Granular updates (like changing a single attribute) require rewriting the entire embedded object mapping. However, since business static attributes rarely change, this write penalty is highly negligible.
\end{itemize}

\textbf{Referencing Trade-offs (Reviews/Checkins/Friends):}
\begin{itemize}
    \item \textbf{Write Advantage:} Excellent write performance and infinite scalability. When a new review is posted, it simply inserts a new document in the Reviews collection without ever locking or modifying the massive parent Business or User document.
    \item \textbf{Read Disadvantage:} Requires application-level joins or aggregation pipeline \texttt{\$lookups}. Fetching a business alongside all its reviews demands querying multiple collections, increasing memory usage and retrieval latency. This trade-off is mitigated by strategic indexing.
\end{itemize}

\subsubsection*{(c) Indexing Strategy for Section 4 Queries}
To efficiently execute the analytical queries mapped out in Section 4 of the assignment, the following targeted indexes were established inside the ETL loading script:

\begin{enumerate}
    \item \texttt{db.businesses.createIndex(\{ "city": 1, "review\_count": -1 \})}
    \begin{itemize}
        \item \textbf{Justification:} Queries 1 and 2 filter and aggregate by city while enforcing minimum review-volume thresholds. This index accelerates those filters and reduces scan volume before grouping.
    \end{itemize}
    \item \texttt{db.businesses.createIndex(\{ "categories": 1, "review\_count": -1 \})}
    \begin{itemize}
        \item \textbf{Justification:} Queries 1, 2, 4, and 7 all touch category-level analysis with review-volume constraints. This multikey index supports category filtering and faster category aggregates.
    \end{itemize}
    \item \texttt{db.reviews.createIndex(\{ "business\_id": 1, "date": 1 \})}
    \begin{itemize}
        \item \textbf{Justification:} Query 2 analyzes rating trends \textit{over time}. This compound index supports extremely fast sequential retrievals natively sorted by date for any given business, removing the need for an expensive in-memory sort stage during the aggregation pipeline.
    \end{itemize}
    \item \texttt{db.reviews.createIndex(\{ "user\_id": 1, "date": 1 \})}
    \begin{itemize}
        \item \textbf{Justification:} Queries 5 and 6 join users to review-level behavior. This compound index speeds user-centric traversals and supports optional temporal slicing.
    \end{itemize}
    \item \texttt{db.users.createIndex(\{ "yelping\_since": 1 \})}
    \begin{itemize}
        \item \textbf{Justification:} Query 5 bins users by tenure on Yelp. This index helps tenure-range filtering and bucketing when user-side prefiltering is applied.
    \end{itemize}
    \item \texttt{db.checkins.createIndex(\{ "\_id": 1 \})} \textit{(Auto-generated mapped from business\_id)}
    \begin{itemize}
        \item \textbf{Justification:} \texttt{checkins} use \texttt{business\_id} directly as their explicit \texttt{\_id}, granting them an automatic, uniquely optimal index at scale. For Query 7 (correlating check-in visit frequency with business star ratings), we natively rely on this \texttt{\_id} index to flawlessly execute a 1:1 optimal pipeline join with the \texttt{businesses} collection on the same key.
    \end{itemize}
\end{enumerate}

\section{Entity-Relationship (E-R) Diagram (MongoDB)}

\subsection*{1. E-R Diagram}
\begin{figure}[H]
        \centering
    \includegraphics[width=0.95\textwidth]{er-diagram.png}
        \caption{Entity-Relationship diagram for the MongoDB data model.}
\end{figure}

\subsection*{2. Explanation of Relationships and Attributes}
\begin{itemize}
        \item \textbf{Embedded relationships:} Business \texttt{hours} and \texttt{attributes} are embedded because they are tightly coupled with one business document and are queried together with business profile fields.
        \item \textbf{Referenced relationships:} Reviews and tips reference users and businesses using \texttt{user\_id} and \texttt{business\_id}, enabling unbounded growth without inflating parent documents.
        \item \textbf{1:1 extension:} Checkins uses \texttt{business\_id} as \texttt{\_id}, creating a direct one-to-one extension document for each business.
        \item \textbf{Self-reference:} User friendships are represented as an array of referenced user IDs (N:M style social links) rather than embedded user objects.
        \item \textbf{Key attributes retained in the diagram:} identifiers, rating fields, temporal fields, text payload fields, and array/object fields that drive the assignment queries.
\end{itemize}

\section{Document Schema Diagram (MongoDB Collections)}

\subsection*{1. Document Schema Diagram}
\begin{figure}[H]
        \centering
        \includegraphics[width=0.95\textwidth]{class-diagram.png}
        \caption{Document schema diagram with fields, types, nesting, and references.}
\end{figure}

\subsection*{2. Structural Design Justifications}
\begin{itemize}
        \item \textbf{Business document keeps bounded nested data embedded:} \texttt{hours} and \texttt{attributes} are naturally one-business scoped and usually read with the business profile.
        \item \textbf{High-volume activity is referenced:} \texttt{reviews} and \texttt{tips} are separate collections to avoid unbounded growth in user/business documents.
        \item \textbf{Checkin separation supports write scalability:} each check-in event appends to \texttt{checkins.dates} without repeatedly rewriting large business documents.
        \item \textbf{User social links use ID arrays:} storing \texttt{friends} as IDs avoids heavy duplication from embedded friend subdocuments and maintains compatibility with graph projection workflows.
        \item \textbf{ETL-consistent typing:} date fields are transformed to Date values in ETL, and categories/friends/elite are normalized into arrays for efficient aggregation and indexing.
\end{itemize}

\section{Neo4j Property Graph Model}

\subsection*{1. Graph Schema Design: Nodes \& Properties}
The Yelp dataset contains dense, multi-hop networking correlations (like Users having Friends who review Businesses in shared Categories). To maximize Cypher traversal performance, the property graph was modeled utilizing four primary Node representations:

\begin{itemize}
    \item \textbf{(User) Node}\\
    \textit{Properties:} \texttt{id} (String, Indexed UUID), \texttt{name} (String), \texttt{review\_count} (Integer), \texttt{yelping\_since} (Date Type), \texttt{average\_stars} (Float).
    
    \item \textbf{(Business) Node}\\
    \textit{Properties:} \texttt{id} (String, Indexed UUID), \texttt{name} (String), \texttt{city} (String), \texttt{state} (String), \texttt{stars} (Float), \texttt{review\_count} (Integer).
    
    \item \textbf{(Review) Node}\\
    \textit{Properties:} \texttt{id} (String, Indexed UUID), \texttt{stars} (Integer), \texttt{date} (Date Type), \texttt{text} (String), \texttt{useful} (Integer).
    
    \item \textbf{(Category) Node}\\
    \textit{Properties:} \texttt{name} (String, Indexed, Unique). (Note: By extracting string arrays from JSON and upgrading them into explicit `Category` nodes, we can trivially query ``all users who reviewed businesses in X category'' using simple path traversals).
\end{itemize}

\subsection*{2. Graph Schema Design: Relationships}
\begin{itemize}
    \item \texttt{(User)-[:FRIENDS\_WITH]->(User)}\\
    \textit{Direction:} Undirected (bi-directional implicit). Represents the N:M social web between users.
    \item \texttt{(User)-[:WROTE]->(Review)}\\
    \textit{Direction:} Directed. Binds a user payload specifically to the rating document.
    \item \texttt{(Review)-[:REVIEWS]->(Business)}\\
    \textit{Direction:} Directed. Assigns the sentiment payload explicitly to the localized entity.
    \item \texttt{(Business)-[:IN\_CATEGORY]->(Category)}\\
    \textit{Direction:} Directed. Defines the classification of a commercial entity (e.g., 'Restaurants', 'Active Life').
\end{itemize}

\subsection*{3. Property Graph Diagram}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{property-graph.png}
    \caption{Neo4j property graph model (labels, directions, and key properties).}
\end{figure}

\subsection*{4. Modelling Choices \& Handling Non-Graph Aspects}

\textbf{Treating Reviews as Nodes rather than Relationships:}\\
Intuitively, a review appears functionally identical to an edge: \texttt{(User)-[:REVIEWED \{stars: 5, date: '...'\}]->(Business)}. However, directly injecting massive payloads (like paragraphs of raw string text and interaction metrics such as \texttt{useful} counts) into edge properties forces the database to evaluate heavy string clusters during large-scale network traversals like Community Detection or PageRank pathing. By extracting the Review into its own definitive \textit{Node}, it maintains a lighter relationship bridging \texttt{User $\rightarrow$ WROTE $\rightarrow$ Review $\rightarrow$ REVIEWS $\rightarrow$ Business}.

\textbf{Omission of Deeply Nested Document Blocks:}\\
Property Graphs strictly require relationships to be bound to flat, scalar nodes. Deeply nested associative JSON arrays (like a business's operational \texttt{hours} map spanning sub-dictionaries of days mapped to time windows, or intricate nested \texttt{attributes}) do not map naturally to nodes or edges without creating severe fragmentation (e.g., mapping a dedicated \texttt{Monday} node).

Because spatial/temporal routing queries based on store opening hours are notoriously better suited to Document stores (like MongoDB parsing embedded JSON), complex dictionary fields like \texttt{hours} were intentionally omitted from the graph translation, prioritizing exclusively the relational ties driving the analytical graph queries defined in Section V.

\end{document}
